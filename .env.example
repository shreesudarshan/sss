# Core backend configuration.
DATABASE_URL=postgresql+asyncpg://sss_user:sss_pass@localhost:5432/sss_db
# Secret used to derive AES encryption key.
AES_KEY=replace-with-strong-random-value
# Secret used for deterministic HMAC search tokens.
HMAC_KEY=replace-with-strong-random-value
# Secret used to hash session tokens.
APP_SECRET=replace-with-strong-random-value
# Session lifetime in hours (168 = 7 days).
SESSION_TTL_HOURS=168
# Allowed frontend origins for browser requests.
CORS_ORIGINS=http://localhost:5173
